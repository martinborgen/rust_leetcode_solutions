
struct Cell {
    row: i32,
    col: i32,
    val: i32,
}

struct Solution;

impl Solution {
    pub fn shortest_path_binary_matrix(grid: Vec<Vec<i32>>) -> i32 {
        
    }

    /// Returns the adjecant cells.
    fn get_adjecants(grid: Vec<Vec<i32>>, origin) -> Vec<i32> {
        
    }
}

fn main() {
    res = Solution::shortest_path_binary_matrix();
}
